"""[HAND-WRITTEN] 부동산 세율·요율 결정표 — 정책값 단일 출처(2026 기준). 순수 데이터, I/O 없음.

cost_model이 이 표를 참조해 계산한다. 법 개정 시 '여기만' 고친다(감사 가능성).
각 값에 법적 근거를 주석으로 남긴다. 보유세/양도세 일부는 명시한 근사다.
"""

from __future__ import annotations

EOK = 100_000_000.0   # 1억(원)

# ── 취득세 (지방세법 §11). 1주택 기준 6억↓1%, 6~9억 선형 1→3%, 9억↑3%. ──
ACQ_EDU_SURTAX = 1.1          # 지방교육세 근사 가산(×1.1)
ACQ_1HOME_LOW = 0.01          # 6억 이하 기본
ACQ_1HOME_HIGH = 0.03         # 9억 초과 기본
ACQ_1HOME_LOW_EOK = 6.0       # 선형 구간 하단(억)
ACQ_1HOME_HIGH_EOK = 9.0      # 선형 구간 상단(억)
ACQ_MULTI_HOME = 0.08         # 다주택(조정지역 2주택) 중과 근사 — 보수적. 농특·교육세 포함 근사.

# ── 중개보수 상한요율 (서울 매매, 2021 개편). (구간상한[억], 요율) ──
BROKERAGE_TABLE = [
    (0.5, 0.006), (2.0, 0.005), (9.0, 0.004),
    (12.0, 0.005), (15.0, 0.006), (float("inf"), 0.007),
]

# ── 보유세(재산세+종부세) 연 실효율 근사, 매매가 대비. 1주택 대략값. (구간상한[억], 율) ──
HOLDING_TAX_TABLE = [
    (9.0, 0.001), (15.0, 0.0025), (25.0, 0.005), (float("inf"), 0.008),
]

# ── 양도소득세 기본세율 누진 (소득세법 §55). (과세표준 상한[원], 한계세율) ──
CGT_BRACKETS = [
    (14_000_000, 0.06), (50_000_000, 0.15), (88_000_000, 0.24),
    (150_000_000, 0.35), (300_000_000, 0.38), (500_000_000, 0.40),
    (1_000_000_000, 0.42), (float("inf"), 0.45),
]
CGT_LOCAL_SURTAX = 1.1            # 지방소득세 10% 가산
CGT_BASIC_DEDUCTION = 2_500_000  # 양도소득 기본공제(연 250만)

# ── 장기보유특별공제 (소득세법 §95). ──
LTD_MIN_HOLD = 3.0               # 일반 공제 개시(보유 3년+)
LTD_GENERAL_PER_YEAR = 0.02      # 일반: 연 2%
LTD_GENERAL_MAX = 0.30           # 일반: 최대 30%
LTD_1HOME_PER_YEAR = 0.04        # 1세대1주택: 보유·거주 각 연 4%
LTD_1HOME_MAX_EACH = 0.40        # 1세대1주택: 보유·거주 각 최대 40%
LTD_1HOME_MAX_TOTAL = 0.80       # 1세대1주택: 합산 최대 80%

# ── 1세대1주택 비과세 (소득세법 §89). 고가주택 기준·최소 보유. ──
HIGH_PRICE_THRESHOLD = 12 * EOK  # 12억 초과분만 과세(안분)
ONE_HOME_MIN_HOLD = 2.0          # 비과세 최소 보유(2년)

# ── 전월세전환율 (주택임대차보호법 §7의2 환산, 한국은행 서울 실측 근사). ──
# 전세보증금을 연 임대료(거주가치)로 환산하는 율. buy-vs-rent에서 자가 거주효용 산정.
RENT_CONVERSION_RATE = 0.04      # 서울 아파트 전월세전환율 근사(2026 기준). 시기·단지별 상이.
